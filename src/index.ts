// import utils
import {
  getThemedColor,
  useThemedColor,
  getBaseColorPair,
  getResolvedColorPair,
} from "./util/tools";
// import theme/overrides
import { theme, overrides } from "./theme";

export {
  // utils
  getThemedColor,
  useThemedColor,
  getBaseColorPair,
  getResolvedColorPair,
  // theme
  theme,
  overrides,
};
